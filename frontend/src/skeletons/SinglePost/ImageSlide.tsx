import React from 'react'

import Skeleton from '@mui/material/Skeleton'

const ImageSlide = () => {
   return <Skeleton animation='wave' variant='rectangular' height={400} width={'100%'} />
}

export default ImageSlide
