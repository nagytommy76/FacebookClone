import { styled } from '@mui/material'

export const EmptyContainer = styled('section')({
   width: '100%',
   height: '100%',
   marginTop: 30,

   display: 'flex',
   flexDirection: 'column',
   alignItems: 'center',
})
