import styled from '@emotion/styled'

export const MainPageContainer = styled('section')({
   position: 'relative',
   width: '100%',
   minHeight: '100%',
   margin: 'auto',

   display: 'flex',
   justifyContent: 'center',
})
